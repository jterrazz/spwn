import { describe, test } from "vitest";

// TODO: requires Docker + running containers to spawn universes
describe.skip("gate bridge", () => {
  test("gate bridges element access into container", () => {});
  test("elements not listed are not available inside universe", () => {});
  test("agent inside universe uses bridged tools", () => {});
  test("gate server is accessible inside the container", () => {});
  test("faculties.md reflects bridged elements", () => {});
});
