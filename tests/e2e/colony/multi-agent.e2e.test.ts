import { describe, test } from "vitest";

// TODO: requires Docker + running containers to spawn universes
describe.skip("colony multi-agent", () => {
  test("spawn universe with governor and citizens", () => {});
  test("add multiple citizens to a universe", () => {});
  test("inspect universe shows all inhabitants", () => {});
  test("governor delegates task to citizens", () => {});
  test("destroying universe cleans up all agents", () => {});
});
