import { describe, test } from "vitest";

// TODO: requires Docker + running containers to spawn universes
describe.skip("universe physics", () => {
  test("universe respects CPU limits from config", () => {});
  test("universe respects memory limits from config", () => {});
  test("universe includes declared elements", () => {});
  test("faculties.md is generated inside the universe", () => {});
});
