import { describe, test } from "vitest";

// TODO: requires Docker + running containers to spawn universes
describe.skip("universe destroy", () => {
  test("destroys a running universe", () => {});
  test("destroy removes universe from list", () => {});
  test("destroy non-existent universe fails", () => {});
});
