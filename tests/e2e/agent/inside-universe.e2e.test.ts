import { describe, test } from "vitest";

// TODO: requires Docker + running containers to spawn universes
describe.skip("agent inside universe", () => {
  test("spawn agent into a universe with -n flag", () => {});
  test("agent receives Mind inside universe container", () => {});
  test("talk to agent inside universe", () => {});
  test("agent without --universe flag uses default behavior", () => {});
});
