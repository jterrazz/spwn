# Agent composition.
# Tools listed here must also be present in the world - this is what
# the agent is allowed to reach for inside the sandbox.

name: neo
runtime: claude-code

tools:
  - "@spwn/unix"
  - "@spwn/git"
  - "@spwn/claude-code"
