name: helly
description: Helly — newest refiner; sharp, skeptical, surfaces the assumption everyone else skipped.
role: worker

runtime:
  backend: "spwn:claude-code"

# Project-wide deps (unix, git) are inherited from spwn.yaml.
# Local blocks are referenced via the skill:/tool:/hook: schemes.
dependencies:
  - "skill:refine"
