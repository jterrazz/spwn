# neo agent configuration
name: neo
role: worker
runtime:
  backend: "spwn:claude-code" # resolves to claude-code compile adapter
  model: opus                  # pinned into .claude/settings.json#model + codex profile
  provider: anthropic
dependencies:
  - "spwn:unix"
