# This file configures the neo agent.
name: neo            # basename under spwn/agents/
role: worker         # default role

# Catalog dependencies and local refs live in a single flat list.
dependencies:
  - "spwn:unix"     # POSIX basics
