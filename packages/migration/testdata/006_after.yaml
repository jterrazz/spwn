name: my-org
version: 1
defaults:
  runtime:
    backend: claude-code
    provider: anthropic
governance:
  max-worlds: 10
  max-workers-per-world: 5
  allowed-providers:
    - anthropic
  cost-limit: "$100"
  audit: true
