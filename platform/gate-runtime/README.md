# Gate Runtime

> Container-side gate — Rust ACP client that bridges Host <-> Agent.

Built with the official `agent-client-protocol` crate. Speaks ACP to the agent CLI (Claude Code) and relays element bridge calls to the Host-side Gate server over TCP.
