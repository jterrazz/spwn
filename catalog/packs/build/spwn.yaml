name: "@spwn/build"
kind: sdk
version: latest
description: "C/C++ build essentials (make, gcc, g++)"

install:
  packages:
    - make
    - gcc
    - g++

verify:
  - command -v make
  - command -v gcc
  - command -v g++
