name: ballmer
description: The hype-maker — writes demos, pitches, and customer-facing copy that sells the work the rest of the colony ships.
role: worker

runtime:
  backend: "spwn:claude-code"

# Local blocks are referenced via the skill/, tool/, hook/ schemes and
# live at the project root (spwn/skills/<name>.md).
dependencies:
  - "skill/sales-pitch"
