name: gates
description: The engineer — reviews code, designs systems, and ships the implementation behind each product decision.
role: worker

runtime:
  backend: "spwn:claude-code"

# Local blocks are referenced via the skill/, tool/, hook/ schemes and
# live at the project root (spwn/skills/<name>.md).
dependencies:
  - "spwn:python"
  - "skill/code-review"
  - "skill/architecture"
