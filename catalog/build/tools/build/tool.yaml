name: "spwn:build"
version: latest
description: "C/C++ build essentials (make, gcc, g++)"

install:
  packages:
    apt:
      - make
      - gcc
      - g++

verify:
  - command -v make
  - command -v gcc
  - command -v g++
