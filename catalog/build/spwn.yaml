# Catalog entry for @spwn/build.
# The actual tool definition lives at tools/build/tool.yaml; this
# file is the project-shape wrapper so the catalog entry mirrors a
# real spwn project layout (spwn.yaml at root + spwn/tools/<name>
# when installed).
version: 2
name: build
