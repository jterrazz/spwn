# Prod — production decisions live here.
# Strict physics, minimum tools, long timeout.

physics:
  constants:
    cpu: 2
    memory: 2g
    disk: 4g
    timeout: 8h

tools:
  - "@spwn/unix"
  - "@spwn/git"
