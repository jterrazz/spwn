name: Default
description: Built-in governor/citizen hierarchy
roles:
  - name: governor
    level: 0
    can_command:
      - citizen
    max_per_world: 1
    permissions:
      - delegate
      - review
      - orchestrate
  - name: citizen
    level: 1
    reports_to: governor
    permissions:
      - execute
      - report
